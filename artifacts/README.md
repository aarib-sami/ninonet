# Model outputs

Keep here for the app / writeup:
- `forecast.json` — Day 4 CNN winter ONI (Day 5 API reads this)
- `enso_cnn_lead6_tuned.pt` — for Day 6 ONNX export (optional until then)
- `skill_curve.png` / `skill_curve_tuned.png` (+ csv) — README / Day 7

Do **not** commit huge caches or full SST nets (gitignored). Re-download from Drive if you need `pacific_anom.nc` again.
